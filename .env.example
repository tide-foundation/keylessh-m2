# Keyle-SSH Environment Configuration
# Copy this file to .env and customize for your environment

# Server
PORT=3000
NODE_ENV=development

# Database (SQLite file path)
DATABASE_URL=./data/keylessh.db

# External TCP Bridge (optional, for production scaling)
# BRIDGE_URL=wss://your-bridge.azurecontainerapps.io
# Note: Bridge authenticates using JWT verified against JWKS (same tidecloak.json)

# Auth server override (optional, for testing)
# AUTH_SERVER_OVERRIDE_URL=http://localhost:8080

# Debug logging (optional)
# DEBUG=true

# Enable SCP debug mode in file browser (shows "Force SCP Mode" option)
# VITE_ENABLE_SCP_DEBUG=true

# ============================================
# Stripe Billing / Licensing (optional - SaaS mode)
# ============================================
# By default, KeyleSSH runs with NO usage limits (unlimited users/servers).
# This is ideal for self-hosting or internal enterprise deployments.
#
# If you want to offer KeyleSSH as a SaaS with tiered billing:
# 1. Configure the Stripe variables below
# 2. The License page will appear in admin navigation
# 3. Tier-based limits will be enforced (Free: 5 users/2 servers, etc.)

# Stripe secret API key (server-side)
# STRIPE_SECRET_KEY=sk_test_...

# Stripe webhook signing secret for /api/webhooks/stripe
# STRIPE_WEBHOOK_SECRET=whsec_...

# Stripe Price IDs (must start with price_, not prod_)
# STRIPE_PRICE_ID_PRO=price_...
# STRIPE_PRICE_ID_ENTERPRISE=price_...

# Base URL used for Stripe success/cancel/return URLs
# APP_URL=http://localhost:5173

# Enterprise is contact-only (no Stripe price required)
# VITE_ENTERPRISE_CONTACT_URL=https://yourcompany.com/contact
