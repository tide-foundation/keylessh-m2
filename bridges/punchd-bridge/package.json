{
  "name": "puncd",
  "version": "0.1.0",
  "private": true,
  "description": "NAT-traversing authenticated reverse proxy — STUN/TURN signaling server + gateway",
  "scripts": {
    "build": "cd gateway && npm run build",
    "start": "cd gateway && npm start"
  }
}
