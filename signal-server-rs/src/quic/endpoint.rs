// QUIC relay removed — browser connections use WebRTC DataChannel.
// QUIC/WebTransport is only used by the native VPN client (punchd-vpn),
// which connects directly to the gateway's wtransport endpoint.
